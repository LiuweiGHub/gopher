高级语言线程模型：

1. N:1模型  n个用户空间线程，1个内核线程。  n个用户线程共享一个内核线程，优势是上下文切换快， 劣势是 无法使用多核优势，只能用一个核
2. 1:1模型  JAVA语言， 1个用户线程，1个内核线程，优势：使用多核，实现并发，上下文切换很麻烦
3. m:n模型  Go语言， 优势：高效实现并发（减少上下文切换），劣势：调度相当复杂


Go的调度器实现：
模型：MPG    P processor  表示调度的上下文

M： 物理态线程
P:  调度器 （逻辑核、上下文）
G:  Go程


/**
查漏补缺基础：
bool类型默认是false